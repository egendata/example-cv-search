{
  "name": "searchcv",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "build-watch": "parcel watch ./index.html",
    "start-watch": "nodemon server/index.js",
    "predev": "npm run build",
    "dev": "egendata tunnel --port=5000 --subdomain=cvsearch --unsafe npm run start-watch npm run build-watch",
    "build": "parcel build ./index.html",
    "start": "npm run build && node server/index.js"
  },
  "author": "Egendata",
  "license": "Apache-2.0",
  "dependencies": {
    "@egendata/client": "1.2.0",
    "react": "16.12.0",
    "react-dom": "16.12.0"
  },
  "devDependencies": {
    "@egendata/cli": "^0.2.0",
    "concurrently": "^5.0.2",
    "nodemon": "^2.0.2",
    "parcel-bundler": "^1.12.4"
  }
}
